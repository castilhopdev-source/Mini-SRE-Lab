# ------------------------------------------------------------
# Monitor Service - Mini SRE Lab
#
# Container responsável por executar o serviço monitor em Python.
# O monitor coleta e expõe métricas (ex: requests, failures)
# para o Prometheus realizar o scraping.
#
# Baseado em python:3.11-slim para manter a imagem leve.
# ------------------------------------------------------------


FROM python:3.11-slim 
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY monitor.py .
CMD ["python", "monitor.py"]  
